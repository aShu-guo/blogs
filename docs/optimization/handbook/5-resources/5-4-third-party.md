# 5.4 第三方脚本管理

## 原则
- 最小必要、异步加载、隔离影响。

## 做法
- 延迟/异步：`async`/`defer`，或按需动态加载
- 资源托管：优先自托管或可信 CDN，开启缓存与压缩
- 运行隔离：在 Worker/iframe 中执行重脚本，限制对主线程影响
- 监控：记录加载/执行耗时和错误率，超过阈值降级或屏蔽

## 场景
- 地图/可视化 SDK：按路由懒加载，拆分插件
- 统计/监控：异步加载，失败不影响主流程
- IM/聊天/客服：仅在对应页面加载
