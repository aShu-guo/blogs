# codex

```shell
npm install -g @openai/codex@latest
```

1. key配置目录：~/.codex/auth.json 用于更新auth key

```json
{
  "OPENAI_API_KEY": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
}
```

2. 其他配置：~/.codex/config.toml，更新模型、MCP等相关配置
3. 添加CODEX.md，添加模型需要遵守的规则
