# Vue3内置指令

| 指令                 | 简写                            | 是否支持表达式 | 是否支持修饰符 | 示例                                             |
| -------------------- | ------------------------------- | -------------- | -------------- | ------------------------------------------------ |
| **v-bind**           | `:`                             | ✔             | ✔             | `:src="imgUrl"`，`:class="{ active: isActive }"` |
| **v-on**             | `@`                             | ✔             | ✔             | `@click="onClick"`，`@keyup.enter="onEnter"`     |
| **v-model**          | **无**（2.x 中 `.sync` 已废弃） | ✔             | ✔             | `v-model="form.name"`                            |
| **v-if**             | 无                              | ✔             | ✘              | `v-if="visible"`                                 |
| **v-else**           | 无                              | ✘              | ✘              | `v-if="isA"` + `v-else`                          |
| **v-else-if**        | 无                              | ✔             | ✘              | `v-else-if="isB"`                                |
| **v-show**           | 无                              | ✔             | ✘              | `v-show="visible"`                               |
| **v-for**            | 无                              | ✔             | ✘              | `v-for="item in list"`                           |
| **v-once**           | 无                              | ✘              | ✘              | `<div v-once>静态内容</div>`                     |
| **v-memo**           | 无                              | ✔             | ✘              | `v-memo="[x, y]"`（3.2+）                        |
| **v-pre**            | 无                              | ✘              | ✘              | `<div v-pre>{{ 不会解析 }}</div>`                |
| **v-html**           | 无                              | ✔             | ✘              | `v-html="htmlString"`                            |
| **v-text**           | 无                              | ✔             | ✘              | `v-text="msg"`                                   |
| **v-cloak**          | 无                              | ✘              | ✘              | 用于避免闪烁：`[v-cloak]{display:none}`          |
| **v-slot**           | `#`                             | ✔             | ✔             | `<template #default="props">`                    |
| **v-bind:xxx.prop**  | `:xxx.prop`                     | ✔             | ✔             | 绑定 DOM 原生属性（如 input.value）              |
| **v-bind:xxx.attr**  | `:xxx.attr`                     | ✔             | ✔             | 强制绑定 attribute                               |
| **v-bind:xxx.camel** | `:xxx.camel`                    | ✔             | ✔             | 转换为驼峰（如 viewBox）                         |
| **v-on:xxx.once**    | `@xxx.once`                     | ✔             | ✔             | 事件只触发一次                                   |
| **v-on:xxx.capture** | `@xxx.capture`                  | ✔             | ✔             | 捕获阶段监听事件                                 |
| **v-on:xxx.passive** | `@xxx.passive`                  | ✔             | ✔             | Passive 事件                                     |

---

# 📌 **常见简写规则总结**

| 原指令   | 简写规则    | 示例                 |
| -------- | ----------- | -------------------- |
| `v-bind` | 用 `:` 代替 | `:class="..."`       |
| `v-on`   | 用 `@` 代替 | `@click="..."`       |
| `v-slot` | 用 `#` 代替 | `<template #header>` |

---

# 📦 **v-model 的多种写法（Vue3 新特性汇总）**

| 写法              | 说明                  | 示例                                  |
| ----------------- | --------------------- | ------------------------------------- |
| `v-model`         | 默认绑定 `modelValue` | `<input v-model="value" />`           |
| `v-model:arg`     | 具名 model            | `<Comp v-model:title="title" />`      |
| `v-model.xxx`     | 修饰符                | `<input v-model.trim="msg" />`        |
| `v-model:arg.xxx` | 具名+修饰符           | `<Comp v-model:title.lazy="title" />` |

修饰符包括：`lazy`、`trim`、`number` 等。

---

# 📌 **更完整的事件修饰符表（Vue3 / DOM 兼容）**

| 修饰符           | 示例              | 说明             |
| ---------------- | ----------------- | ---------------- |
| `.stop`          | `@click.stop`     | 阻止事件冒泡     |
| `.prevent`       | `@submit.prevent` | 阻止默认行为     |
| `.capture`       | `@click.capture`  | 捕获阶段         |
| `.once`          | `@click.once`     | 触发一次后解绑   |
| `.passive`       | `@scroll.passive` | Passive listener |
| `.self`          | `@click.self`     | 自身触发才执行   |
| `.left`/`.right` | `@click.left`     | 鼠标按键         |
| `.enter` 等键位  | `@keyup.enter`    | keyboard events  |
| `.exact`         | `@click.exact`    | 修饰符精确匹配   |

---

# 📦 **适合你的博客的代码块模板**

如果你要写博客，我可以帮你生成这样的章节：

### **1. Vue3 所有内置指令分类图**

（结构图 + 分类）

### **2. 简写语法与语法糖总结**

（包括示例、错误用法对比）

### **3. 各指令的底层实现解析（源码级别）**

如：`v-model` → `transformModel()`
`v-on` → `transformOn()`
`v-bind` → `transformBind()`
等。

---

如果你需要，我可以继续帮你生成：

- **Markdown 表格版本**（更适合直接粘贴到博客）
- **“Vue3 指令体系详解”整篇文章骨架**
- **源码级别的 transform / patch 行为流程图**

你需要哪个？
